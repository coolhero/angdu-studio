# Runtime Exploration Results

> Generated by `/reverse-spec` Phase 1.5 — 2026-03-15T15:00:00Z
> Mode: automated (Playwright CLI — _electron.launch())
> Target: /Users/coolhero/Develop/cherry-studio

## App-Wide Observations

### Initial Setup Requirements
- **In-app configuration**: Settings → Model Provider → API key entry. Multiple providers pre-configured (CherryIN, SiliconFlow, AiHubMix, etc.)
- **Setup completed**: partial — API key exists in .env, CherryIN provider active

### Navigation Structure
- **Top-level nav**: Tab-based navbar at top (44px height). Tabs: Home (always visible) + dynamically added tabs for Translate, Paintings, Knowledge Base, Notes, Files, History
- **Sub-navigation patterns**: Home page has left panel with Assistants/Topics tabs. Settings has left sidebar nav with categorized items
- **Routing scheme**: Hash routing (#/settings, #/translate, #/paintings, etc.)

### Global UI Patterns
- **Component library**: Ant Design (antd) — visible in button classes, form controls. Styled Components for layout
- **Theme/color scheme**: Light mode by default (body class="light", html class="light"). CSS variables for theming
- **Responsive behavior**: Fixed window 960×600 default, resizable
- **Common layout patterns**: Tab navbar (top) + content area. Content often split into left panel + main area

### Runtime Defaults (VERIFIED via Playwright)
- **navbarPosition**: `"top"` (tab mode — NOT sidebar mode)
- **theme**: `"light"` (body.className = "light")
- **hasSidebar**: false (top mode has no app sidebar — only per-page left panels)
- **hasNavbar**: true (44px height, 738px width in content area)
- **window size**: 960×600 (inner), devicePixelRatio: 2

---

## Screen: #/ — Home (Chat)

**Layout**: Top navbar (tabs) + Content (left panel 210px + chat area 738px)

### UI Elements
- **Navbar**: Home tab with icon + "+" button for new tab. Right side: mini-window icon + settings gear icon
- **Left Panel** (210px): Assistants/Topics toggle tabs (button pair). "+ Add Assistant" button. Draggable assistant list (Default Assistant shown)
- **Chat Area**: Navbar with assistant name + model selector. Message area. Input bar at bottom with rich toolbar
- **Input Toolbar**: File upload, web search, knowledge base, MCP servers, model select, quick phrases, clear context (⌘+K)
- **Message Area**: Default assistant greeting message

### User Flows from This Screen
| Flow | Steps | Observations |
|------|-------|--------------|
| Send message | Type in input → click send or press Enter | Input bar with TipTap editor |
| Switch assistant | Click assistant in left panel | Assistants tab active by default |
| View topics | Click "Topics" tab in left panel | Switches from assistant list to topic list |
| Add assistant | Click "+ Add Assistant" | Opens assistant creation |
| Change model | Click model name in chat navbar | Model selector dropdown |
| Open settings | Click gear icon (top-right) | Navigates to #/settings |

### Runtime Behavior
- **Loading states**: Chat messages load with streaming
- **Empty states**: Default assistant with greeting message
- **Console errors**: None observed

---

## Screen: #/settings — Settings

**Layout**: Left sidebar nav (full height) + right content area

### UI Elements
- **Left sidebar categories** (in order):
  1. Model Provider
  2. Default Model
  3. ---separator---
  4. General Settings
  5. Display Settings
  6. Data Settings
  7. ---separator---
  8. MCP Servers
  9. Web Search
  10. Memories
  11. API Server
  12. Document Processing
  13. Quick Phrases
  14. Keyboard Shortcuts
  15. Quick Assistant
  16. Selection Assistant
  17. About & Feedback
  18. CherryIN
- **Content area**: Provider configuration with API key input, API host input, provider logos
- **Pre-configured providers**: CherryIN (ON), SiliconFlow, AiHubMix, ocoolAI, BigModel, Z.ai, DeepSeek, Alaya, DMXAPI, AiOnly, BurnCloud, TokenFlux, 302.AI, Cephalon, LANYUN + "Add" button

### Notes
- Settings navigation is a separate left sidebar (not the same as app-level sidebar)
- Model Provider is the default settings page (#/settings/provider)

---

## Screen: #/translate — Translate

**Layout**: Top toolbar + split-pane (source panel + target panel)

### UI Elements
- **Toolbar**: OCR button, source language selector ("Auto Detect"), swap button, target language selector (English flag), "Translate" button, model selector, settings icon
- **Source panel**: Text input area with "Text, text files, or images (with OCR support) can be pasted or dragged in". Character count (192)
- **Target panel**: "Translation" label, output area
- **Bottom-left**: Green "+" FAB button

---

## Screen: #/paintings — Paintings (Image Generation)

**Layout**: Provider selector + generation area

### UI Elements
- Provider selector, "Learn More" link
- Empty state: "No available image generation model, please add a model and set the endpoint type to Image Generation (OpenAI)"
- "Go to Settings" link
- Draw/Edit tabs

---

## Screen: #/knowledge — Knowledge Base

**Layout**: Header with "Add" button + list area

### UI Elements
- Empty state: "No knowledge base found"
- Add button for creating knowledge bases

---

## Screen: #/notes — Notes

**Layout**: Note list + editor area

### UI Elements
- Drag-drop zone: "Drop .md files or folders here to import"
- Empty state: "No notes available yet"

---

## Screen: #/files — Files

**Layout**: Tab bar + file list with sort controls

### UI Elements
- Tabs: Document, Image, Text, All Files
- Sort controls: Created At, Size, Name
- "Select All" checkbox
- Empty state: "No data"

---

## Screen: #/history — History

**Layout**: Search/filter area + history list

### UI Elements
- Chat history list view

---

## Runtime Default Verification

| Setting | Code Analysis | Runtime Value | Status |
|---------|--------------|---------------|--------|
| navbarPosition | (not yet analyzed) | `"top"` | ✅ Runtime captured |
| theme | (not yet analyzed) | `"light"` | ✅ Runtime captured |
| hasSidebar | (not yet analyzed) | `false` (top mode) | ✅ Runtime captured |
| navbarHeight | (not yet analyzed) | `44px` | ✅ Runtime captured |
| windowSize | (not yet analyzed) | `960×600` | ✅ Runtime captured |
| assistantsWidth | (not yet analyzed) | `210px` | ✅ Runtime captured |
