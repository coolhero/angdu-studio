# Runtime Exploration Results

> Generated by `/reverse-spec` Phase 1.5 — 2026-03-08T07:15:00Z
> Mode: automated (Playwright via CDP)
> Target: /Users/coolhero/Develop/cherry-studio

## App-Wide Observations

### Initial Setup Requirements
- **In-app configuration**: Settings → Model Provider → API Key entry per provider, model selection
- **Setup completed**: yes — Gemini and Anthropic providers configured with API keys

### Navigation Structure
- **Top-level nav**: Tab bar at top — Home (always), then dynamically opened tabs (Knowledge Base, Paintings, Translate, Notes, Apps, Files, Code, OpenClaw, Assistant Library)
- **Tab system**: Each app opens as a new tab; tabs are closable with × icon; + button opens Launchpad to add new tabs
- **Sub-navigation patterns**: Left sidebar within each tab (Assistants/Topics in Home, category lists in Store, file types in Files, note tree in Notes)
- **Routing scheme**: Hash routing (`/#/route`)
- **Settings access**: Gear icon in top-right corner → full-page settings with left sidebar navigation

### Global UI Patterns
- **Component library**: Ant Design 5 (buttons, inputs, dropdowns, switches, tabs, modals, alerts)
- **Theme/color scheme**: Dark theme active (toggleable via moon/sun icon). Dark gray backgrounds (#1a1a1a range), green accents for primary actions
- **Icon system**: Mix of Ant Design icons and Lucide React icons
- **Common layout patterns**:
  - Sidebar + content (Home chat, Settings, Store, Knowledge, Files)
  - Three-column (Settings → Provider list → Provider config)
  - Split-pane (Translate: source | target)
  - Full-width centered (Launchpad, Code Tools)
  - Card grid (Apps/Mini Apps)
- **Empty states**: Consistent pattern with gray illustration icon + descriptive text + optional CTA button
- **Tab overflow**: Scrollable tab bar when many tabs are open; left/right scroll arrows appear

---

## Screen: /#/ — Home (Chat)

**Layout**: Sidebar (Assistants/Topics) + Main chat area

### UI Elements
- **Left sidebar**:
  - Tab switch: Assistants | Topics
  - "+ Add Assistant" button
  - Assistant list with emoji avatar, name, context menu (⋮)
- **Chat header**:
  - Sidebar toggle icon
  - Assistant name with emoji
  - Model selector dropdown (e.g., "Gemini 2.5 Flash | Gemini")
  - Settings icon (chat parameters)
  - Search icon
- **Chat area**: Welcome message from assistant, scrollable message area
- **Input bar**:
  - Text input with placeholder "Type your message here, press Enter to send"
  - Toolbar row 1 (left): Quick action, Attach file, Reasoning effort, Web Search, URL Context, Knowledge Base, MCP Servers, Select Model
  - Separator
  - Toolbar row 2 (right): Quick Phrases, two unnamed buttons, Clear Context (⌘+K)
  - Expand button (fullscreen input)
  - Send button (disabled when empty)
  - Language translate button (bottom-right)

### User Flows from This Screen
| Flow | Steps | Observations |
|------|-------|--------------|
| Send message | Type → Enter or Send button | Send disabled until text entered |
| Switch assistant | Click assistant in sidebar | Loads different assistant context |
| Create topic | New message creates auto-topic | Topics tab shows conversation history |
| Model selection | Click model dropdown in header | Provider + model picker |
| Web search toggle | Click Web Search in toolbar | Enables web search for next message |
| Attach files | Click attachment icon | File upload dialog |

### Runtime Behavior
- **Loading states**: Message streaming with typing animation
- **Empty states**: Welcome message from assistant
- **Keyboard shortcuts**: ESC Close, ↑↓ Select, ↩ Confirm (shown in command palette hint)

---

## Screen: /#/settings/provider — Settings > Model Provider

**Layout**: Settings sidebar + Provider list (middle) + Provider config (right)

### UI Elements
- **Settings sidebar**: 15 navigation items in 4 groups separated by dividers:
  1. Model Provider, Default Model
  2. General Settings, Display Settings, Data Settings
  3. MCP Servers, Web Search, Memories, API Server, Document Processing, Quick Phrases, Keyboard Shortcuts
  4. Quick Assistant, Selection Assistant
  5. About & Feedback
- **Provider list**: Searchable grid of ~15+ providers (Gemini, Anthropic, OpenAI, CherryIN, SiliconFlow, AiHubMix, etc.) with ON/OFF badges, + Add button
- **Provider config**: Toggle switch, API Key input (masked), Check button, API Host, Model list (grouped by version), Manage/Add buttons, documentation links

### Notes
- Multiple API keys supported (comma-separated)
- API Host customizable with preview URL
- Models grouped by version family (Gemini 2.5, Gemini 3)
- Each model shows capability badges (eye=vision, globe=web, tool=function-calling)

---

## Screen: /#/settings/mcp — Settings > MCP Servers

**Layout**: Settings sidebar + MCP server list (middle) + Server config (right)

### UI Elements
- **MCP server list**:
  - Discover section: Builtin Servers, Marketplaces
  - Providers: Alibaba Cloud, ModelScope, TokenFlux, LANYUN, 302.AI, MCP Router
- **Server management**: Search, Edit (JSON), Add buttons
- **Empty state**: "No servers configured"

---

## Screen: /#/launchpad — Launchpad (App Launcher)

**Layout**: Full-width centered grid

### UI Elements
- Heading "Apps"
- 3×3 grid of app icons with labels:
  - Apps, Knowledge Base, Paintings, Assistant Library, Translate, Files, Code, OpenClaw, Notes
- Each icon is a colored square with symbolic icon

### Notes
- This is the app launcher that opens when clicking + in the tab bar
- Clicking an app opens it as a new tab

---

## Screen: /#/knowledge — Knowledge Base

**Layout**: Sidebar (knowledge base list) + Content area

### UI Elements
- "+ Add" button in sidebar
- Empty state: "No knowledge base found" with icon

### Notes
- RAG-based knowledge base using embeddings (embedjs library)
- Supports multiple document formats (PDF, CSV, Markdown, Web, XML, Image, MS Office)

---

## Screen: /#/paintings/cherryin — Paintings (Image Generation)

**Layout**: Three-panel — Provider selector (left) + Canvas/preview (center) + Gallery (right)

### UI Elements
- **Left panel**: Provider dropdown (CherryIN default), empty state when no models configured
- **Center**: Draw/Edit toggle tabs, image preview area ("No image available"), prompt input textarea
- **Right panel**: Image gallery with + button, delete button
- **Bottom**: Send button, translate button

### Notes
- Supports text-to-image and image editing modes
- Provider-specific image generation models
- CherryIN is the default but requires model configuration

---

## Screen: /#/translate — Translate

**Layout**: Split-pane (source | target) with toolbar

### UI Elements
- **Toolbar**: Settings icon, Source language dropdown ("Auto Detect"), Swap button (disabled), Target language dropdown ("English" with flag), Translate button
- **Left pane**: Input textarea with character count (192 limit shown), supports text/file/image paste with OCR
- **Right pane**: Translation output with copy button
- **Top-right**: Model selector, settings icon

---

## Screen: /#/notes — Notes

**Layout**: Sidebar (note tree) + Content (editor)

### UI Elements
- **Sidebar toolbar**: 5 icons (create, import, tree view, favorites, search)
- **Content area**: Toolbar with notebook icon and menu (⋯), empty state "No notes available yet"
- **Bottom**: Drop zone "Drop .md files or folders here to import"

### Notes
- Markdown-based note system with TipTap editor
- Supports .md file import via drag-and-drop
- Sidebar supports tree/folder organization, favorites, search

---

## Screen: /#/apps — Mini Apps

**Layout**: Full-width card grid with search

### UI Elements
- Search input with settings gear
- Grid of 40+ web app shortcuts: ChatGPT, Gemini, SiliconFlow, DeepSeek, Kimi, Cici, Groq, Claude, Google, ERNIE, Yuanbao, Metaso, Poe, Perplexity, DEVV_, Felo, DuckDuckGo, bolt, ThinkAny, GitHub Copilot, Genspark, Grok, QwenChat, Flowith, 3MinTop, AI Studio, NotebookLM, Coze, Dify, LeChat, Abacus, Lambda Chat, Monica, You, Dangbei, Z.ai, n8n, LongCat, Ant Ling, HuggingChat, + Custom
- Each app shows icon + name, clickable to open in embedded webview

---

## Screen: /#/files — Files

**Layout**: Sidebar (file type filter) + Content (file list)

### UI Elements
- **Sidebar**: Document, Image, Text, All Files categories
- **Toolbar**: Sort by Created At/Size/Name, Select All checkbox, ellipsis menu
- **Content**: Empty state "No data"

### Notes
- File manager for uploaded/attached files
- Supports sorting and bulk selection

---

## Screen: /#/code — Code Tools

**Layout**: Full-width centered form

### UI Elements
- Heading "Code Tools" with description
- Warning alert: "Bun environment is required" + Install Bun button
- Form fields: CLI Tool dropdown (Qwen Code), Model selector, Working Directory (with Select Folder button), Environment Variables textarea, Auto-update checkbox
- Launch button (disabled until configured)

### Notes
- Launches external code CLI tools (like Qwen Code, Claude Code, etc.)
- Requires Bun runtime
- Each tool runs in a selected working directory with configurable env vars

---

## Screen: /#/store — Assistant Library

**Layout**: Sidebar (categories) + Content (agent list)

### UI Elements
- **Sidebar categories** with counts: My Agents (0), Featured (0), Career (273), Business (162), Tools (283), Language (25), Office (44), General (37), Writing (121), Featured (4), Programming (61), Emotion (57)
- **Toolbar**: Search, Import from External, Manage Assistants, Create Assistant buttons
- **Content**: Empty in "My Agents" tab

### Notes
- Large pre-built assistant marketplace with 1000+ agents across categories
- Support for creating custom assistants, importing from external sources
- "Featured" tab for curated picks

---

## Screen: /#/openclaw — OpenClaw

**Layout**: Full-width

### UI Elements
- Loading state: "Checking OpenClaw installation..."

### Notes
- External tool integration (OpenClaw)
- Requires separate installation check
