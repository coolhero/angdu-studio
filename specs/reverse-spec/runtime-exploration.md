# Runtime Exploration Results

> Generated by `/reverse-spec` Phase 1.5 — 2026-03-15T12:25:00+09:00
> Mode: automated (Playwright CLI via CDP)
> Target: /Users/coolhero/Develop/cherry-studio

## App-Wide Observations

### Initial Setup Requirements
- **In-app configuration**: API provider keys must be configured in Settings > Model Provider before chat works
- **Setup completed**: Partial — Gemini 2.5 Flash pre-configured as default model

### Navigation Structure
- **Top-level nav**: Tab bar at top — "Home" tab + "+" button for new tabs (Code, Knowledge, Files, Translate, Paintings, Notes, MiniApps)
- **Sub-navigation patterns**: Left sidebar with "Assistants" / "Topics" toggle tabs; each has its own list view
- **Routing scheme**: Hash-based single-page routing within Electron renderer
- **Settings**: Gear icon (top-right) opens settings modal/page
- **Theme toggle**: Moon/sun icon (top-right) cycles through dark/light/system

### Global UI Patterns
- **Component library**: Ant Design 5 (detected from class names: `ant-*`)
- **Theme/color scheme**: Dark theme by default (system-following), primary green (#00b96b)
- **Window frame**: macOS hidden title bar with traffic light buttons (titleBarStyle: 'hidden')
- **Background**: Semi-transparent with vibrancy effect (sidebar vibrancy)
- **Responsive behavior**: Not observed (desktop-only, min 1080x600)
- **Common layout patterns**: Sidebar (210px) + main content; tab bar at top (44px height)

### Code-only routes (detected from source, not navigated)
- `/settings/*` — Settings pages (General, Model Provider, Assistant, Data, About, Display, Shortcuts)
- `/code` — Code workspace
- `/knowledge` — Knowledge base management
- `/files` — File browser
- `/translate` — Translation tool
- `/paintings` — Image generation
- `/notes` — Notes/Documents
- `/minapps` — Mini applications
- `/store` — Plugin store
- `/history` — Chat history browser
- `/launchpad` — App launcher

---

## Screen: / — Home (Chat)

**Layout**: Sidebar (210px, left) + Chat content (right), Tab bar (44px, top)

### UI Elements
- **Tab bar**: "Home" tab (active), "+" button to add new tab, dark mode toggle, settings gear
- **Left sidebar**:
  - "Assistants" / "Topics" toggle tabs
  - "+ Add Assistant" button
  - Assistant list with emoji avatars and context menu (3-dot)
- **Chat header**:
  - Collapse sidebar toggle (left)
  - Current assistant name + emoji
  - Model selector (dropdown: "Gemini 2.5 Flash | Gemini")
  - Settings icon, Search icon (right)
- **Chat area**: Message display area (scrollable)
  - System message: "Hello, I'm Default Assistant. You can start chatting with me right away"
- **Message input**:
  - Textarea: "Type your message here, press Enter to send"
  - Toolbar icons (left): New topic, Attach file, Knowledge base, Web search, Link, Image OCR, @ Mention, MCP tools
  - Toolbar icons (right): Translate, Send button

### User Flows from This Screen
| Flow | Steps | Observations |
|------|-------|--------------|
| Start chat | Type message → Enter | Sends to selected model |
| Switch assistant | Click assistant in sidebar | Changes chat context |
| New topic | Click "+" in toolbar or "+ New Topic" | Creates new conversation thread |
| Switch model | Click model selector dropdown | Shows available models |
| Open settings | Click gear icon (top-right) | Opens settings page |
| Add tab | Click "+" in tab bar | Shows page selection menu |

### Runtime Behavior
- **Loading states**: Splash screen with Cherry Studio logo on initial load
- **Empty states**: Default assistant with greeting message
- **Error handling**: Not observed (no errors triggered)
- **Console errors**: None (5 warnings: Electron security, deprecated API, provider override)

### Notes
- Window uses macOS vibrancy effect (sidebar)
- Dark theme is the default (system-following)
- Default window size: 960x600, min: 1080x600
- Tab-based multi-session support (each tab can be a different page type)

---

## Runtime Default Verification

| Setting | Code Analysis | Runtime Value | Status |
|---------|--------------|---------------|--------|
| Theme | dark (system) | dark (system-following) | Confirmed |
| Primary color | #00b96b | #00b96b | Confirmed |
| Window size | 1080x600 min | 960x600 (default) | Confirmed |
| Sidebar width | ~210px | 210px | Confirmed |
| Navbar height | 44px | 44px | Confirmed |
| Font size | 14px | 14px | Confirmed |
| Background | semi-transparent vibrancy | rgba(20,20,20,0.55) with vibrancy | Confirmed |
| Sidebar tabs | Assistants/Topics | Assistants (active) / Topics | Confirmed |
