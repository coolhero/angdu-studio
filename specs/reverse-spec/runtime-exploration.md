# Runtime Exploration Results

> Generated by `/reverse-spec` Phase 1.5 — 2026-03-14T14:45:00
> Mode: automated (Playwright CLI, _electron.launch())
> Target: /Users/coolhero/Develop/cherry-studio

## App-Wide Observations

### Initial Setup Requirements
- **In-app configuration**: Settings → Model Provider → Add API key for AI providers (e.g., CherryIN, OpenAI, Anthropic)
- **Setup completed**: Partial — CherryIN provider configured with API key

### Navigation Structure
- **Top-level nav**: Tab bar at top — Home, Assistant Library, Files, Notes, Knowledge Base, Paintings, Translate, Code, Apps, + (add tab), Settings (gear icon), Mini window (monitor icon)
- **Sub-navigation patterns**: Left sidebar within each page (Assistants/Topics tabs on Home, note tree in Notes, etc.)
- **Routing scheme**: Hash-based routing (`#/route`)

### Global UI Patterns
- **Component library**: Ant Design 5 components (buttons, modals, selectors), styled-components for layout
- **Theme/color scheme**: Dark mode default (--color-background: #181818), green primary (#00b96b), light text on dark
- **Responsive behavior**: Fixed window, 960×600 default, supports resizing
- **Common layout patterns**: Top navbar + left sidebar + main content area. Some pages use split-pane (Translate). Settings uses left nav menu + right content.

---

## Screen: #/ — Home (Chat)

**Layout**: navbar + left-sidebar (Assistants/Topics) + main chat area

### UI Elements
- Left sidebar: Assistants tab (selected, green underline), Topics tab, "+ Add Assistant" button, assistant list with emoji icons and context menu (⋮)
- Top bar: Collapse sidebar toggle, assistant avatar + name ("Default Assistant"), model selector dropdown ("Qwen3-Next-80B | CherryIN"), settings icon, search icon
- Chat area: Message display with greeting message, message input area at bottom
- Input toolbar: Add to prompt (⊞), Attach file (📎), Web search (🌐), Knowledge base (📚), Image paste (📋), @ mention, Code block (>_)
- Bottom right: Translate icon, Send button (green arrow)

### User Flows from This Screen
| Flow | Steps | Observations |
|------|-------|--------------|
| Chat with AI | Type message → Press Enter → AI responds | Streaming response display |
| Switch assistant | Click assistant in sidebar → Chat context changes | |
| Switch model | Click model dropdown → Select model | Model provider + model name shown |
| Create new topic | Click Topics tab → New conversation | |

### Runtime Behavior
- **Loading states**: Chat messages stream progressively
- **Empty states**: Default greeting "Hello, I'm Default Assistant. You can start chatting with me right away"
- **Error handling**: Toast notifications for API errors

---

## Screen: #/settings — Settings

**Layout**: full-width left nav menu + right content area

### UI Elements
- Left nav menu items (with icons):
  - Model Provider, Default Model
  - General Settings, Display Settings, Data Settings
  - MCP Servers, Web Search, Memories
  - API Server, Document Processing
  - Quick Phrases, Keyboard Shortcuts
  - Quick Assistant (partially visible, scrollable)
- Right content area: Form controls for selected setting category

### Runtime Behavior
- **Layout pattern**: Settings sidebar is separate from main app sidebar

---

## Screen: #/store — Assistant Library

**Layout**: navbar + grid/card layout for assistant presets

### UI Elements
- Category tabs/filters
- Assistant preset cards with icons and descriptions
- Import/export functionality

---

## Screen: #/files — Files

**Layout**: navbar + file browser

### UI Elements
- Search input at top
- File list with file names, types
- Upload/add button

---

## Screen: #/notes — Notes

**Layout**: navbar + left sidebar (note tree) + editor area

### UI Elements
- Left toolbar: New note, New folder, Import, Star/favorites, Search (5 icon buttons)
- Toggle sidebar button
- Main area: "No notes available yet" empty state
- Bottom: "Drop .md files or folders here to import" drop zone
- More options (⋯) menu

### Runtime Behavior
- **Empty states**: Folder icon illustration with "No notes available yet" message
- **Drag-drop**: Supports .md file drag-and-drop import

---

## Screen: #/knowledge — Knowledge Base

**Layout**: navbar + left sidebar (knowledge base list) + detail area

### UI Elements
- "+ Add" button at top of sidebar
- Empty state: "No knowledge base found"

---

## Screen: #/paintings — Paintings (Image Generation)

**Layout**: navbar + left sidebar (provider) + center canvas + right thumbnails

### UI Elements
- Provider selector (CherryIN dropdown) with "Learn More" link
- Draw/Edit toggle tabs
- Canvas area with "No image available" empty state
- Thumbnail gallery on right side
- Bottom input: "Enter your image description, text drawing uses 'double quotes' to wrap"
- Translate icon + Send button (green)

### Runtime Behavior
- **Empty states**: Printer illustration with "No available image generation model, please add a model..." message + "Go to Settings" CTA button

---

## Screen: #/translate — Translate

**Layout**: navbar + toolbar + split-pane

### UI Elements
- OCR button (camera icon)
- Source language selector (Auto Detect dropdown)
- Swap languages button (↕)
- Target language selector (English dropdown)
- Translate button (▷ Translate)
- Model selector icon + settings icon
- Split pane: Left = source text input (with placeholder "Text, text files, or images (with OCR support) can be pasted or dragged in"), Right = translation output
- Character count at bottom (192)

---

## Screen: #/code — Code Tools

**Layout**: navbar + "Code Tools" heading + code editor area

### UI Elements
- Code editor with input fields
- Language/model selection

---

## Screen: #/apps — Mini Apps

**Layout**: navbar + app grid

### UI Elements
- Search input
- App cards/list

---

## Screen: #/launchpad — Launchpad

**Layout**: full-width grid with "Apps" heading

### UI Elements
- App launcher grid

---

## Runtime Default Verification

| Setting | Code Analysis | Runtime Value | Status |
|---------|--------------|---------------|--------|
| Theme | dark | dark | ✅ Confirmed |
| Primary color | #00b96b | #00b96b | ✅ Confirmed |
| Default window size | 960×600 | 960×600 | ✅ Confirmed |
| Navigation style | Top tab bar | Top tab bar | ✅ Confirmed |
| Font family | Ubuntu, system fonts | Ubuntu, system fonts | ✅ Confirmed |
| Routing scheme | Hash-based | Hash-based (#/) | ✅ Confirmed |
