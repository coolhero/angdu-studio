# Runtime Exploration Results

> Generated by `/reverse-spec` Phase 1.5 — 2026-03-13T06:00:00Z
> Mode: automated (Playwright CLI, _electron.launch)
> Target: /Users/coolhero/Develop/cherry-studio

## App-Wide Observations

### Initial Setup Requirements
- **In-app configuration**: Settings → AI Provider API key entry, model selection
- **Setup completed**: partial — .env has API_KEY configured, in-app provider setup not performed during exploration

### Navigation Structure
- **Top-level nav**: Icon-based vertical sidebar (left) with configurable icons
- **Sidebar icons** (configurable via settings): assistants (chat), store (presets), paintings (AI art), translate, minapp (apps grid), knowledge, files, notes, code_tools, openclaw
- **Bottom sidebar**: Theme toggle (dark/light/system), Settings gear
- **Sub-navigation patterns**: Tabs within pages (Assistants/Topics in chat, sub-routes in Settings)
- **Routing scheme**: HashRouter (react-router-dom v6)

### Routes (from source code)
| Route | Page Component | Description |
|-------|---------------|-------------|
| `/` | HomePage | Main chat interface with assistants/topics |
| `/store` | AssistantPresetsPage | Assistant presets marketplace |
| `/paintings/*` | PaintingsRoutePage | AI image generation |
| `/translate` | TranslatePage | Translation tool |
| `/files` | FilesPage | File management |
| `/notes` | NotesPage | Notes/documents |
| `/knowledge` | KnowledgePage | Knowledge base management |
| `/apps/:appId` | MinAppPage | Individual mini-app |
| `/apps` | MinAppsPage | Mini-apps grid |
| `/code` | CodeToolsPage | Code tools |
| `/openclaw` | OpenClawPage | OpenClaw integration |
| `/settings/*` | SettingsPage | Settings (nested routes) |
| `/launchpad` | LaunchpadPage | Launchpad |

### Global UI Patterns
- **Component library**: Ant Design 5 (ant-btn, ant-modal, css-var- classes)
- **CSS approach**: styled-components + Ant Design CSS-in-JS + CSS custom properties
- **Theme/color scheme**: Dark mode (primary: #00b96b green), supports light/dark/system
- **Layout pattern**: Icon sidebar (35px icons, round) + content area
- **Window**: Custom titlebar (frameless, -webkit-app-region: drag), macOS style
- **Sidebar width**: CSS variable `--sidebar-width`
- **Navbar height**: CSS variable `--navbar-height`
- **Font**: Ubuntu / system fonts stack, 14px base

### Multiple Windows
- **Main window**: Full app with sidebar + routes
- **Mini window**: Compact chat window (separate entry point)
- **Selection toolbar**: Text selection floating toolbar
- **Trace window**: OpenTelemetry trace viewer

---

## Screen: / — Main Chat (HomePage)

**Layout**: sidebar + left-panel (assistants/topics list) + main-content (chat area)

### UI Elements
- **Left panel**: Assistants/Topics tab switcher, search input, "+ Add Assistant" button, assistant list with emoji avatars and context menu (⋮)
- **Top bar**: Collapse sidebar toggle, current assistant name + emoji, model selector dropdown (breadcrumb: "Default Assistant > Qwen3-Next-80B | CherryIN")
- **Top right**: Settings/filter icon, search icon
- **Chat area**: Message bubbles with assistant greeting, empty state
- **Input area**: Textarea ("Type your message here, press Enter to send")
- **Input toolbar**: 7 action icons from left to right:
  - New topic (⊞)
  - File attachment (📎) — supports documents, tooltip shows model image support status
  - Web search (🌐)
  - Knowledge base (📚) — popup with Clear/Add options
  - MCP servers (🚀) — popup with Disabled/Auto/Manual modes
  - @ mentions
  - Code/commands (>)
- **Input right**: Translation toggle (文A), Send button (↑)

### User Flows from This Screen
| Flow | Steps | Observations |
|------|-------|--------------|
| Send message | Type in textarea → Enter or click Send | Primary chat flow |
| Switch assistant | Click assistant in left panel | Changes current assistant context |
| Switch model | Click model name in top bar → dropdown | Model selection dropdown appears |
| New topic | Click ⊞ icon or use context menu | Creates new conversation topic |
| Attach file | Click 📎 → file picker | Document upload for context |
| Web search toggle | Click 🌐 → toggle | Enables web search for queries |
| Knowledge base | Click 📚 → popup | Add/clear knowledge bases |
| MCP toggle | Click 🚀 → popup (Disabled/Auto/Manual) | MCP tool injection control |

### Runtime Behavior
- **Loading states**: Not observed (no active AI requests during exploration)
- **Empty states**: "Hello, I'm Default Assistant. You can start chatting with me right away"
- **Console errors**: 0
- **Window dimensions**: 960×600 default

---

## Screen: /settings/* — Settings

**Layout**: sidebar + settings navigation + settings content (from source code analysis)

### UI Elements (from code analysis)
- Settings has nested routes with categories
- Provider settings (default route: `/settings/provider`)
- Web search settings
- MCP settings with provider configs
- General, display, shortcut settings

---

## Screen: /store — Assistant Presets

**Layout**: Grid/list of preset assistants

### UI Elements
- Assistant preset cards
- Categories/search

---

## Screen: /paintings — AI Art

**Layout**: Painting interface with provider-specific sub-routes

### UI Elements
- Provider selector
- Image generation controls
- Gallery

---

## Screen: /translate — Translation

**Layout**: Translation interface

---

## Screen: /knowledge — Knowledge Base

**Layout**: Knowledge base management

### UI Elements
- Knowledge base list
- Add/manage embeddings
- Document ingestion

---

## Screen: /apps — Mini Apps

**Layout**: Grid of mini-applications

### UI Elements
- App cards grid
- Pinnable apps (appear in sidebar)

---

## Screen: /files — File Management

**Layout**: File browser

---

## Screen: /notes — Notes

**Layout**: Notes editor (TipTap-based rich text)

---

## Screen: /code — Code Tools

**Layout**: Code tools interface

---

## MCP Servers Popup (from chat toolbar)

**Layout**: Bottom popup panel

### UI Elements
- Three modes: Disabled (No MCP tools ✓), Auto (AI discovers and uses tools automatically), Manual (Select specific MCP servers >)
- Keyboard navigation: ESC Close, ▲▼ Select, ⌘+▲▼ Page, ⌘+◄► Back/Forward, ↵ Confirm

---

## Knowledge Base Popup (from chat toolbar)

**Layout**: Bottom popup panel

### UI Elements
- Clear option (Clear selected knowledge bases)
- "+ Add Knowledge Base..." button
- Keyboard navigation: ESC Close, ▲▼ Select, ⌘+▲▼ Page, ↵ Confirm
