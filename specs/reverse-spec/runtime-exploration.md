# Runtime Exploration Results

> Generated by `/reverse-spec` Phase 1.5 — 2026-03-14T00:25:47Z
> Mode: automated (Playwright CLI, _electron.launch())
> Target: /Users/coolhero/Develop/cherry-studio

## App-Wide Observations

### Initial Setup Requirements
- **In-app configuration**: API provider setup required (Settings → Model Provider → enter API key). Default model is Qwen3-Next-80B via CherryIN.
- **Setup completed**: partial — app launches with Default Assistant but needs API key for actual chat functionality

### Navigation Structure
- **Top-level nav**: Tab-based navbar at top — Home, Assistant Library, Translate, Files, Notes, Knowledge Base, + (add tab)
- **Sub-navigation patterns**: Left sidebar within Home (Assistants/Topics tabs), Settings page has sidebar menu
- **Routing scheme**: Hash router (`#/`)
- **Tab system**: Persistent tabs across navigation, new pages open as tabs

### Global UI Patterns
- **Component library**: Ant Design 5 + Styled Components (sc-* class prefixes) + Tailwind CSS
- **Theme/color scheme**: Light mode default (body class "light"), dark mode available. Primary color: `#00b96b` (green)
- **Layout**: Top navbar (44px) + content area. No left icon sidebar in default mode — tabs serve as primary navigation
- **Common layout patterns**: Sidebar+content (Home chat, Settings, Store, Notes, Knowledge), Split-pane (Translate), Full-width (Files, Apps)
- **Window dimensions**: 2560x1600 (Retina)
- **Typography**: 14px base, system font stack (Ubuntu, -apple-system, etc.)
- **Scrollbar**: Custom thin scrollbar (5px width, 10px thumb radius)

### CSS Custom Properties (Key Tokens)
- `--color-primary`: `#00b96b`
- `--navbar-height`: `44px`
- `--sidebar-width`: `50px`
- `--assistants-width`: `275px`
- `--topic-list-width`: `275px`
- `--settings-width`: `250px`
- `--status-bar-height`: `40px`
- `--input-bar-height`: `100px`
- `--radius`: `.625rem`
- `--font-family`: system font stack
- `--code-font-family`: Cascadia Code, Fira Code, Consolas, monospace

---

## Screen: #/ — Home (Chat)

**Layout**: Top navbar + Left sidebar (Assistants list, 275px) + Main chat area + Input bar at bottom

### UI Elements
- **Top navbar**: Home tab (active), + button for new tab, Mini window button, Settings gear icon
- **Left sidebar**: Two tabs (Assistants / Topics), "+ Add Assistant" button, list of assistants with emoji icons and context menu (⋮)
- **Chat header**: Assistant name breadcrumb (Default Assistant > Model selector dropdown), config/filter/search icons
- **Chat area**: Message bubbles, system greeting message
- **Input bar**: Text input ("Type your message here, press Enter to send"), toolbar icons: prompt library, attachments, web search, file upload, knowledge base, @ mentions, code block, translate button, send button

### User Flows from This Screen
| Flow | Steps | Observations |
|------|-------|--------------|
| New conversation | Click + Add Assistant → Select assistant → Type message → Send | Creates new topic under assistant |
| Model switching | Click model dropdown in header → Select model | Changes model for current conversation |
| Topic management | Click Topics tab → Select topic | Switches between conversation threads |

### Runtime Behavior
- **Loading states**: Greeting message appears immediately
- **Empty states**: "Hello, I'm Default Assistant. You can start chatting with me right away"
- **Input toolbar**: 8 action buttons for attachments, web search, file upload, knowledge, mentions, code, translate

---

## Screen: #/store — Assistant Library

**Layout**: Top navbar with tabs + Left sidebar (我的/精选 tabs = My/Featured) + Main content area

### UI Elements
- **Left sidebar**: "我的" (My, 0 items) and "精选" (Featured, 0 items) tabs with counts
- **Header**: "我的 0" count, Search, Import from External, Manage Assistants, + Create Assistant buttons
- **Content**: Empty state — "No results found" with illustration
- **Tab system**: Opens as new tab alongside Home

### Runtime Behavior
- **Empty states**: Illustration + "No results found" text
- **Navigation**: Appears as a new tab in the top navbar

---

## Screen: #/translate — Translate

**Layout**: Split-pane — Left (source text) + Right (translation output)

### UI Elements
- **Header bar**: Model icon, Auto Detect dropdown, swap button (⇄), English dropdown, Translate button, model selector, settings icon
- **Left pane**: Text input area with placeholder "Text, text files, or images (with OCR support) can be pasted or dragged in", character count (192)
- **Right pane**: "Translation" label, output area (light gray background)

### Runtime Behavior
- **Split pane**: 50/50 horizontal split
- **OCR support**: Accepts images for text extraction before translation

---

## Screen: #/files — Files

**Layout**: Full-width content area (no sidebar visible in this view)

### Notes
- Opens as tab in navbar
- File management interface (not captured in detail due to empty state)

---

## Screen: #/notes — Notes

**Layout**: Left sidebar (toolbar + note list) + Main content area (editor)

### UI Elements
- **Left sidebar toolbar**: 4 icon buttons (create, import, sort, favorites, search)
- **Left sidebar content**: Note list area, "Drop .md files or folders here to import" at bottom
- **Main area**: Sidebar toggle button, empty state "No notes available yet"
- **Top right**: "..." menu button

### Runtime Behavior
- **Empty states**: "No notes available yet" with folder illustration
- **Import**: Supports .md file drag-and-drop import

---

## Screen: #/knowledge — Knowledge Base

**Layout**: Left sidebar (knowledge base list) + Main content area

### UI Elements
- **Left sidebar**: "+ Add" button, list of knowledge bases
- **Main area**: Empty state — "No knowledge base found" with folder illustration

### Runtime Behavior
- **Empty states**: "No knowledge base found" illustration

---

## Screen: #/settings — Settings

**Layout**: Left sidebar menu (250px) + Main content area

### UI Elements — Settings Menu Structure
**Provider & Model Group:**
- Model Provider
- Default Model

**App Settings Group:**
- General Settings
- Display Settings
- Data Settings

**Features Group:**
- MCP Servers
- Web Search
- Memories
- API Server
- Document Processing
- Quick Phrases
- Keyboard Shortcuts

**Tools Group:**
- Quick Assistant
- Selection Assistant

**Info Group:**
- About & Feedback

### Notes
- Settings sidebar organized into logical groups with visual separators
- Each item has an icon prefix
- Content area loads the selected settings page

---

## Screen: #/apps — Mini Apps

**Layout**: Grid/list of mini applications

### Notes
- Browsable mini-app store
- Each app runs within the main window

---

## Screen: #/paintings — Paintings (Image Generation)

### Notes
- AI image generation feature
- Multiple provider support (detected from code: Aihubmix, Dmxapi, NewApi, Ovms, Ppio, Silicon, TokenFlux, Zhipu)

---

## Screen: #/code — Code Tools

### Notes
- Code-related utilities
- Python execution via Pyodide (WebAssembly)

---

## Screen: #/launchpad — Launchpad

### Notes
- Feature discovery/launcher page
