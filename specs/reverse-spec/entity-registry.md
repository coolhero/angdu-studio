# Entity Registry

**Project**: Angdu Studio (rebuild of Cherry Studio)
**Source**: `/Users/coolhero/Develop/cherry-studio`
**Total Entities**: 20+
**Generated**: 2026-03-07

> Canonical index of every data entity discovered during deep analysis of Cherry Studio.
> Organized by storage layer: Dexie (client-side IndexedDB), Drizzle ORM (server-side LibSQL), and Domain Types (TypeScript interfaces).

---

## 1. Storage Layer Summary

| Layer | Technology | Tables/Types | Process | Persistence |
|-------|-----------|-------------|---------|-------------|
| Dexie | IndexedDB via Dexie 4 | 9 tables | Renderer | Client-local, offline-first |
| Drizzle ORM | LibSQL | 4 tables | Main (API server) | Server-side, optional |
| Domain Types | TypeScript interfaces | 7 types | Both | Not persisted directly; shape data in stores and IPC |

---

## 2. Dexie Tables (Client-side IndexedDB)

### 2.1 Topic

| Attribute | Value |
|-----------|-------|
| Owner | F003-chat |
| Referenced By | F004-editor, F012-extensions |
| Storage | Dexie (IndexedDB) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| type | string | Yes | — | Topic type discriminator |
| assistantId | string | Yes | — | FK to Assistant (domain type) |
| name | string | Yes | — | Display name |
| createdAt | number | Yes | Date.now() | Creation timestamp |
| updatedAt | number | Yes | Date.now() | Last update timestamp |
| messages | Message[] | No | [] | Embedded message references |
| pinned | boolean | No | false | Whether the topic is pinned to top |
| prompt | string | No | — | System prompt override for this topic |
| isNameManuallyEdited | boolean | No | false | Prevents auto-rename when true |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-assistant | assistantId | No | List topics for an assistant |
| by-updated | updatedAt | No | Sort by recency |

#### Relationships

- **Assistant** (N:1) — each Topic belongs to one Assistant via `assistantId`
- **Message** (1:N) — a Topic contains many Messages via `messages` array or `topicId` on Message

#### Validation Rules

- `name` must be non-empty string, max 200 characters
- `assistantId` must reference a valid Assistant

---

### 2.2 Message

| Attribute | Value |
|-----------|-------|
| Owner | F003-chat |
| Referenced By | F004-editor, F007-knowledge, F011-memory |
| Storage | Dexie (IndexedDB) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| role | string | Yes | — | One of: user, assistant, system |
| assistantId | string | Yes | — | FK to Assistant |
| topicId | string | Yes | — | FK to Topic |
| createdAt | number | Yes | Date.now() | Creation timestamp |
| updatedAt | number | Yes | Date.now() | Last update timestamp |
| status | string | Yes | "pending" | One of: pending, streaming, success, error, paused |
| modelId | string | No | — | Model identifier used for generation |
| model | object | No | — | Snapshot of Model metadata at generation time |
| type | string | No | — | Message type discriminator |
| useful | boolean | No | null | User feedback: helpful or not |
| askId | string | No | — | Links a user message to its assistant response |
| mentions | object[] | No | [] | @-mentioned assistants or context |
| usage | object | No | — | Token usage: promptTokens, completionTokens, totalTokens |
| metrics | object | No | — | Performance: latency, tokensPerSecond, timeToFirstToken |
| blocks | MessageBlock[] | No | [] | Ordered list of content blocks |
| traceId | string | No | — | OpenTelemetry trace correlation |
| agentSessionId | string | No | — | FK to agent Session if generated by an agent |
| providerMetadata | object | No | — | Provider-specific metadata blob |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-topic | topicId | No | List messages in a topic |
| by-assistant | assistantId | No | List messages for an assistant |
| by-created | createdAt | No | Chronological ordering |
| by-ask | askId | No | Link question-answer pairs |

#### Relationships

- **Topic** (N:1) — each Message belongs to one Topic via `topicId`
- **Assistant** (N:1) — each Message is associated with an Assistant via `assistantId`
- **MessageBlock** (1:N) — a Message contains ordered blocks via `blocks` array
- **Model** (N:1, snapshot) — `model` is a denormalized snapshot, not a live FK

#### Validation Rules

- `role` must be one of: user, assistant, system
- `status` must be one of: pending, streaming, success, error, paused
- `topicId` must reference a valid Topic
- `blocks` array order is significant (determines render order)

---

### 2.3 MessageBlock

| Attribute | Value |
|-----------|-------|
| Owner | F003-chat |
| Referenced By | F004-editor, F006-mcp (ToolBlock), F007-knowledge (CitationBlock) |
| Storage | Dexie (IndexedDB), embedded in Message |

#### Fields (Common)

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| messageId | string | Yes | — | FK to parent Message |
| type | string | Yes | — | Block type discriminator (12 variants) |
| createdAt | number | Yes | Date.now() | Creation timestamp |
| status | string | No | — | Block-level status |
| model | object | No | — | Model used for this block (if different from message-level) |
| metadata | object | No | — | Type-specific metadata |
| error | object | No | — | Error details if block failed |

#### Block Type Variants

| Variant | Type Value | Additional Fields | Feature Ref |
|---------|-----------|-------------------|-------------|
| MainText | "MainText" | content (string) | F003-chat |
| Thinking | "Thinking" | content (string), thinking (string) | F003-chat |
| Translation | "Translation" | content (string), sourceLanguage, targetLanguage | F012-extensions |
| Code | "Code" | content (string), language (string) | F004-editor |
| Image | "Image" | url (string), base64 (string), metadata (dimensions, format) | F003-chat |
| Tool | "Tool" | toolName (string), toolArgs (object), toolResult (object), serverId (string) | F006-mcp |
| File | "File" | fileId (string), fileName (string), fileType (string) | F008-file-management |
| Video | "Video" | url (string), metadata (object) | F003-chat |
| Error | "Error" | error (object), code (string) | F003-chat |
| Citation | "Citation" | knowledgeBaseId (string), sourceChunks (object[]), score (number) | F007-knowledge |
| Compact | "Compact" | content (string), isCollapsed (boolean) | F003-chat |
| Placeholder | "Placeholder" | placeholderType (string) | F003-chat |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-message | messageId | No | List blocks for a message |

#### Relationships

- **Message** (N:1) — each block belongs to one Message via `messageId`
- **MCPServer** (N:1, via ToolBlock) — ToolBlock references an MCP server via `serverId`
- **KnowledgeBase** (N:1, via CitationBlock) — CitationBlock references a knowledge base via `knowledgeBaseId`
- **FileMetadata** (N:1, via FileBlock) — FileBlock references a file via `fileId`

#### Validation Rules

- `type` must be one of the 12 defined variants
- Variant-specific fields are required when that variant is active
- `content` must be a string (may be empty during streaming)

---

### 2.4 FileMetadata

| Attribute | Value |
|-----------|-------|
| Owner | F008-file-management |
| Referenced By | F003-chat, F007-knowledge |
| Storage | Dexie (IndexedDB) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| name | string | Yes | — | Display file name |
| origin_name | string | Yes | — | Original file name before rename |
| path | string | Yes | — | Absolute path on disk |
| size | number | Yes | — | File size in bytes |
| ext | string | Yes | — | File extension (e.g., ".pdf", ".txt") |
| type | string | Yes | — | MIME type or file category |
| created_at | number | Yes | Date.now() | Creation timestamp |
| count | number | No | — | Page or item count (for documents) |
| tokens | number | No | — | Estimated token count after preprocessing |
| purpose | string | No | — | Usage context: chat-attachment, knowledge-doc, export |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-type | type | No | Filter by file type |
| by-created | created_at | No | Sort by recency |

#### Relationships

- **MessageBlock** (1:N) — FileBlock references FileMetadata via `fileId`
- **KnowledgeNoteItem** (1:N) — knowledge items may reference source files

#### Validation Rules

- `path` must be an absolute path
- `size` must be >= 0
- `ext` must start with "."

---

### 2.5 KnowledgeNoteItem

| Attribute | Value |
|-----------|-------|
| Owner | F007-knowledge |
| Referenced By | F003-chat |
| Storage | Dexie (IndexedDB) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| baseId | string | Yes | — | FK to KnowledgeBase (domain type) |
| type | string | Yes | — | Item type: document, note, url |
| content | string | Yes | — | Raw content or extracted text |
| sourceUrl | string | No | — | Source URL for web-sourced items |
| remark | string | No | — | User annotation |
| created_at | number | Yes | Date.now() | Creation timestamp |
| updated_at | number | Yes | Date.now() | Last update timestamp |
| processingStatus | string | No | "pending" | One of: pending, processing, completed, error |
| processingProgress | number | No | 0 | 0-100 percentage |
| processingError | string | No | — | Error message if processing failed |
| retryCount | number | No | 0 | Number of processing retries |
| isPreprocessed | boolean | No | false | Whether content has been chunked and embedded |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-base | baseId | No | List items in a knowledge base |
| by-status | processingStatus | No | Filter by processing state |

#### Relationships

- **KnowledgeBase** (N:1) — each item belongs to one knowledge base via `baseId`

#### Validation Rules

- `processingStatus` must be one of: pending, processing, completed, error
- `processingProgress` must be 0-100
- `content` must be non-empty

---

### 2.6 TranslateHistory

| Attribute | Value |
|-----------|-------|
| Owner | F012-extensions |
| Referenced By | — (standalone) |
| Storage | Dexie (IndexedDB) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| sourceText | string | Yes | — | Original text |
| targetText | string | Yes | — | Translated text |
| sourceLanguage | string | Yes | — | Source language code |
| targetLanguage | string | Yes | — | Target language code |
| createdAt | number | Yes | Date.now() | Creation timestamp |
| star | boolean | No | false | User-starred for quick access |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-created | createdAt | No | Sort by recency |
| by-star | star | No | Filter starred items |

#### Relationships

- None (standalone entity)

#### Validation Rules

- `sourceText` and `targetText` must be non-empty
- `sourceLanguage` and `targetLanguage` must be valid language codes

---

### 2.7 QuickPhrase

| Attribute | Value |
|-----------|-------|
| Owner | F003-chat |
| Referenced By | — (standalone) |
| Storage | Dexie (IndexedDB) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| title | string | Yes | — | Short label for the phrase |
| content | string | Yes | — | Full phrase text inserted on selection |
| createdAt | number | Yes | Date.now() | Creation timestamp |
| updatedAt | number | Yes | Date.now() | Last update timestamp |
| order | number | No | 0 | Sort order for manual arrangement |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-order | order | No | Custom sort |

#### Relationships

- None (standalone entity)

#### Validation Rules

- `title` must be non-empty, max 100 characters
- `content` must be non-empty

---

### 2.8 CustomTranslateLanguage

| Attribute | Value |
|-----------|-------|
| Owner | F012-extensions |
| Referenced By | — (standalone) |
| Storage | Dexie (IndexedDB) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| langCode | string | Yes | — | ISO language code |
| value | string | Yes | — | Display name for the language |
| emoji | string | No | — | Flag or icon emoji |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-code | langCode | Yes | Lookup by language code |

#### Relationships

- None (standalone entity)

#### Validation Rules

- `langCode` must be a valid ISO language code
- `langCode` must be unique

---

### 2.9 Settings (KV Store)

| Attribute | Value |
|-----------|-------|
| Owner | F001-app-core |
| Referenced By | All features |
| Storage | Dexie (IndexedDB) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | — | Setting key (see ConfigKeys entity) |
| value | any | Yes | — | Setting value (JSON-serializable) |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by key |

#### Relationships

- **ConfigKeys** — `id` values correspond to ConfigKeys enum members

#### Validation Rules

- `id` must be a recognized ConfigKeys value
- `value` type depends on the specific key (enforced at application layer)

---

## 3. Drizzle ORM Tables (Server-side LibSQL)

### 3.1 Agent

| Attribute | Value |
|-----------|-------|
| Owner | F010-agent |
| Referenced By | F003-chat |
| Storage | Drizzle ORM (LibSQL) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| type | string | Yes | — | Agent type discriminator |
| name | string | Yes | — | Agent display name |
| description | string | No | — | Agent description |
| accessible_paths | string[] | No | [] | File system paths the agent can access |
| instructions | string | No | — | System instructions for the agent |
| model | string | No | — | Primary model identifier |
| plan_model | string | No | — | Model used for planning steps |
| small_model | string | No | — | Lightweight model for simple tasks |
| mcps | string[] | No | [] | MCP server IDs the agent can use |
| allowed_tools | string[] | No | [] | Whitelisted tool names |
| configuration | object | No | {} | Additional configuration blob |
| created_at | number | Yes | Date.now() | Creation timestamp |
| updated_at | number | Yes | Date.now() | Last update timestamp |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-type | type | No | Filter by agent type |
| by-name | name | No | Search by name |

#### Relationships

- **Session** (1:N) — an Agent has many Sessions via `agent_id` FK on Session
- **MCPServer** (N:M) — agent references MCP servers via `mcps` array

#### Validation Rules

- `name` must be non-empty, max 200 characters
- `accessible_paths` entries must be absolute paths
- `allowed_tools` entries must be valid tool identifiers

---

### 3.2 Session

| Attribute | Value |
|-----------|-------|
| Owner | F010-agent |
| Referenced By | F003-chat |
| Storage | Drizzle ORM (LibSQL) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Primary key |
| agent_type | string | Yes | — | Denormalized from parent Agent |
| agent_id | string | Yes | — | FK to Agent |
| name | string | No | — | Session display name |
| description | string | No | — | Session description |
| accessible_paths | string[] | No | [] | Override of Agent paths for this session |
| instructions | string | No | — | Override of Agent instructions |
| model | string | No | — | Override of Agent model |
| plan_model | string | No | — | Override of Agent plan model |
| small_model | string | No | — | Override of Agent small model |
| mcps | string[] | No | [] | Override of Agent MCP servers |
| allowed_tools | string[] | No | [] | Override of Agent allowed tools |
| slash_commands | string[] | No | [] | Custom slash commands for this session |
| configuration | object | No | {} | Session-level configuration overrides |
| created_at | number | Yes | Date.now() | Creation timestamp |
| updated_at | number | Yes | Date.now() | Last update timestamp |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Lookup by ID |
| by-agent | agent_id | No | List sessions for an agent |
| by-created | created_at | No | Sort by recency |

#### Relationships

- **Agent** (N:1) — each Session belongs to one Agent via `agent_id`
- **SessionMessage** (1:N) — a Session contains many SessionMessages via `session_id` FK

#### Validation Rules

- `agent_id` must reference a valid Agent
- Override fields (model, instructions, etc.) fall back to Agent values when null

---

### 3.3 SessionMessage

| Attribute | Value |
|-----------|-------|
| Owner | F010-agent |
| Referenced By | — |
| Storage | Drizzle ORM (LibSQL) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | integer | Yes | auto-increment | Primary key |
| session_id | string | Yes | — | FK to Session |
| role | string | Yes | — | One of: user, assistant, system, tool |
| content | string | Yes | — | Message content (may be JSON for tool messages) |
| agent_session_id | string | No | — | Sub-session identifier for multi-turn agent flows |
| metadata | object | No | {} | Additional metadata blob |
| created_at | number | Yes | Date.now() | Creation timestamp |
| updated_at | number | Yes | Date.now() | Last update timestamp |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | id | Yes | Auto-increment lookup |
| by-session | session_id | No | List messages in a session |
| by-created | created_at | No | Chronological ordering |

#### Relationships

- **Session** (N:1) — each SessionMessage belongs to one Session via `session_id`

#### Validation Rules

- `role` must be one of: user, assistant, system, tool
- `session_id` must reference a valid Session
- `content` must be non-empty

---

### 3.4 Migration

| Attribute | Value |
|-----------|-------|
| Owner | F010-agent |
| Referenced By | — (internal) |
| Storage | Drizzle ORM (LibSQL) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| version | string | Yes | — | Primary key; semantic version of the migration |
| tag | string | No | — | Human-readable migration tag |
| executedAt | number | Yes | Date.now() | When the migration was applied |

#### Indexes

| Index | Fields | Unique | Purpose |
|-------|--------|--------|---------|
| Primary | version | Yes | Track applied migrations |

#### Relationships

- None (internal migration tracking)

#### Validation Rules

- `version` must be unique and follow semantic versioning

---

## 4. Domain Types (TypeScript Interfaces)

These types are not directly persisted in a database table. They define the shape of data used in Zustand stores, IPC payloads, and in-memory state.

### 4.1 Assistant

| Attribute | Value |
|-----------|-------|
| Owner | F003-chat |
| Referenced By | F002-ai-provider, F007-knowledge, F009-settings-ui |
| Storage | Zustand store (persisted via localStorage) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Unique identifier |
| name | string | Yes | — | Display name |
| prompt | string | No | — | System prompt |
| emoji | string | No | — | Display emoji/icon |
| description | string | No | — | User-facing description |
| model | object | No | — | Default model configuration |
| defaultModel | object | No | — | Fallback model if primary unavailable |
| topics | string[] | No | [] | Associated Topic IDs |
| knowledge_bases | string[] | No | [] | Associated KnowledgeBase IDs |
| settings | object | No | {} | Assistant-level settings (temperature, topP, maxTokens, etc.) |
| enableWebSearch | boolean | No | false | Whether web search is enabled |
| mcpMode | string | No | — | MCP integration mode |
| mcpServers | string[] | No | [] | Enabled MCP server IDs |
| tags | string[] | No | [] | User-defined categorization tags |
| enableMemory | boolean | No | false | Whether long-term memory is enabled |

#### Relationships

- **Topic** (1:N) — an Assistant owns many Topics via `topics` array
- **KnowledgeBase** (N:M) — an Assistant references knowledge bases via `knowledge_bases`
- **Model** (N:1) — primary model and default model references
- **MCPServer** (N:M) — enabled MCP servers via `mcpServers`

---

### 4.2 Model

| Attribute | Value |
|-----------|-------|
| Owner | F002-ai-provider |
| Referenced By | F003-chat, F007-knowledge, F010-agent, F011-memory |
| Storage | Zustand store (runtime, refreshed from provider) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | — | Model identifier (e.g., "gpt-4o", "claude-sonnet-4-20250514") |
| provider | string | Yes | — | Provider ID this model belongs to |
| name | string | Yes | — | Human-readable model name |
| group | string | No | — | Model family or group |
| owned_by | string | No | — | Organization that owns the model |
| description | string | No | — | Model description |
| capabilities | object | No | {} | Feature flags: vision, function_calling, streaming, json_mode |
| pricing | object | No | — | Per-token pricing: input, output, cached |
| endpoint_type | string | No | — | API endpoint type for routing |

#### Relationships

- **Provider** (N:1) — each Model belongs to one Provider via `provider`

---

### 4.3 KnowledgeBase

| Attribute | Value |
|-----------|-------|
| Owner | F007-knowledge |
| Referenced By | F003-chat |
| Storage | Zustand store (persisted via localStorage) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Unique identifier |
| name | string | Yes | — | Display name |
| description | string | No | — | User-facing description |
| model | string | No | — | Embedding model identifier |
| dimensions | number | No | — | Embedding vector dimensions |
| items | KnowledgeNoteItem[] | No | [] | Contained items |
| created_at | number | Yes | Date.now() | Creation timestamp |
| updated_at | number | Yes | Date.now() | Last update timestamp |
| version | number | No | 1 | Schema version for migration |
| documentCount | number | No | 0 | Number of documents |
| chunkSize | number | No | 500 | Text chunk size for splitting |
| chunkOverlap | number | No | 50 | Overlap between chunks |
| threshold | number | No | 0.7 | Similarity threshold for search results |
| rerankModel | string | No | — | Reranking model identifier |

#### Relationships

- **KnowledgeNoteItem** (1:N) — contains many items
- **Assistant** (N:M) — referenced by assistants via `knowledge_bases`
- **Model** (N:1) — uses a model for embeddings via `model`

---

### 4.4 MCPServer

| Attribute | Value |
|-----------|-------|
| Owner | F006-mcp |
| Referenced By | F003-chat, F010-agent |
| Storage | Zustand store (persisted via localStorage) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | UUID | Unique identifier |
| name | string | Yes | — | Server display name |
| description | string | No | — | Server description |
| type | string | Yes | — | Transport type: stdio, sse, streamableHttp |
| baseUrl | string | No | — | Base URL for SSE/HTTP transports |
| registryUrl | string | No | — | Registry URL for discovery |
| command | string | No | — | Command for stdio transport |
| args | string[] | No | [] | Command arguments for stdio |
| env | object | No | {} | Environment variables for the server process |
| headers | object | No | {} | HTTP headers for SSE/HTTP transports |
| provider | string | No | — | Provider identifier |
| providerUrl | string | No | — | Provider homepage URL |
| tags | string[] | No | [] | Categorization tags |
| longRunning | boolean | No | false | Whether the server is long-running |
| timeout | number | No | 30000 | Request timeout in milliseconds |
| disabledTools | string[] | No | [] | Tools disabled by the user |
| isActive | boolean | No | true | Whether the server is currently enabled |

#### Relationships

- **Assistant** (N:M) — referenced by assistants via `mcpServers`
- **Agent** (N:M) — referenced by agents via `mcps`
- **MessageBlock** (1:N) — ToolBlock references server via `serverId`

---

### 4.5 Plugin

| Attribute | Value |
|-----------|-------|
| Owner | F010-agent |
| Referenced By | — |
| Storage | File system (loaded at runtime) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| sourcePath | string | Yes | — | Absolute path to plugin source |
| filename | string | Yes | — | Plugin file name |
| name | string | Yes | — | Display name |
| description | string | No | — | Plugin description |
| allowed_tools | string[] | No | [] | Tools this plugin provides |
| tools | object[] | No | [] | Tool definitions |
| category | string | No | — | Plugin category |
| type | string | No | — | Plugin type discriminator |
| tags | string[] | No | [] | Categorization tags |
| version | string | No | — | Semantic version |
| author | string | No | — | Plugin author |

#### Relationships

- **Agent** (N:M) — agents may use plugin tools

---

### 4.6 ConfigKeys

| Attribute | Value |
|-----------|-------|
| Owner | F001-app-core |
| Referenced By | All features |
| Storage | Dexie Settings KV store + Zustand store |

#### Fields (Enum Members)

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| Language | string | "en" | UI language code |
| Theme | string | "auto" | Theme mode: light, dark, auto |
| LaunchToTray | boolean | false | Minimize to tray on launch |
| Tray | boolean | true | Show tray icon |
| TrayOnClose | boolean | false | Minimize to tray on close |
| ZoomFactor | number | 1.0 | UI zoom level |
| Shortcuts | object | {} | Keyboard shortcut mappings |
| AutoUpdate | boolean | true | Enable auto-update checks |
| Proxy | string | "" | HTTP/SOCKS proxy URL |
| EnableDeveloperMode | boolean | false | Show developer tools and options |
| ClientId | string | UUID | Unique client installation ID |

(Additional keys exist for provider-specific settings, editor preferences, and feature flags.)

---

### 4.7 Provider

| Attribute | Value |
|-----------|-------|
| Owner | F002-ai-provider |
| Referenced By | F003-chat, F009-settings-ui |
| Storage | Zustand store (persisted via localStorage) |

#### Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| id | string | Yes | — | Unique provider identifier |
| name | string | Yes | — | Display name (e.g., "OpenAI", "Anthropic") |
| type | string | Yes | — | Provider type for routing |
| apiKey | string | No | — | API key (encrypted at rest) |
| apiHost | string | No | — | Custom API host URL |
| models | Model[] | No | [] | Available models for this provider |
| enabled | boolean | No | true | Whether the provider is active |
| isSystem | boolean | No | false | Whether this is a built-in provider |

#### Relationships

- **Model** (1:N) — a Provider owns many Models via `models`

#### Validation Rules

- `apiKey` must never be logged or exported in plaintext
- `apiHost` must be a valid URL when provided

---

## 5. Cross-Entity Relationship Map

```
Provider (1:N) --> Model
                     |
Assistant (N:1) -----+ (via model/defaultModel)
    |
    +-- (1:N) --> Topic (1:N) --> Message (1:N) --> MessageBlock
    |                                                  |
    +-- (N:M) --> KnowledgeBase (1:N) --> KnowledgeNoteItem
    |                                        |
    +-- (N:M) --> MCPServer <--- ToolBlock --+
                     |                   CitationBlock ---> KnowledgeBase
                     |
Agent (N:M) ---------+
  |
  +-- (1:N) --> Session (1:N) --> SessionMessage

Settings (KV) <--- ConfigKeys (enum)

FileMetadata <--- FileBlock (MessageBlock variant)
                  KnowledgeNoteItem (source reference)

TranslateHistory (standalone)
QuickPhrase (standalone)
CustomTranslateLanguage (standalone)
Migration (internal, standalone)
```

---

## 6. Cross-Feature Entity Ownership

| Entity | Owner Feature | Read By | Write By |
|--------|--------------|---------|----------|
| Topic | F003-chat | F004, F012 | F003 |
| Message | F003-chat | F004, F007, F011 | F003 |
| MessageBlock | F003-chat | F004, F006, F007 | F003 |
| FileMetadata | F008-file-management | F003, F007 | F008 |
| KnowledgeNoteItem | F007-knowledge | F003 | F007 |
| TranslateHistory | F012-extensions | — | F012 |
| QuickPhrase | F003-chat | — | F003 |
| CustomTranslateLanguage | F012-extensions | — | F012 |
| Settings | F001-app-core | All | F001, F009 |
| Agent | F010-agent | F003 | F010 |
| Session | F010-agent | F003 | F010 |
| SessionMessage | F010-agent | — | F010 |
| Migration | F010-agent | — | F010 |
| Assistant | F003-chat | F002, F007, F009 | F003, F009 |
| Model | F002-ai-provider | F003, F007, F010, F011 | F002 |
| KnowledgeBase | F007-knowledge | F003 | F007 |
| MCPServer | F006-mcp | F003, F010 | F006, F009 |
| Plugin | F010-agent | — | F010 |
| ConfigKeys | F001-app-core | All | F001 |
| Provider | F002-ai-provider | F003, F009 | F002, F009 |

---

**Version**: 0.1.0 | **Generated**: 2026-03-07
