# Runtime Exploration Results

> Generated by `/reverse-spec` Phase 1.5 — 2026-03-21
> Mode: automated (Playwright CLI, _electron.launch)
> Target: /Users/coolhero/Develop/cherry-studio

## App-Wide Observations

### Initial Setup Requirements
- **In-app configuration**: Model Provider API key 설정, 프로바이더 선택
- **Setup completed**: yes — CherryIN 프로바이더 활성, API key 설정됨

### Navigation Structure
- **Top-level nav**: 탭 기반 navbar (상단). 기본 탭: Home, 추가 가능 (+)
- **Navbar 우측 아이콘**: 미니윈도우, 설정(gear)
- **Available pages**: Home(chat), Knowledge Base, Files, Translate, Paintings, Notes, history, Code Tools, Store(assistants)
- **Routing scheme**: Hash routing (`#/route`)
- **Tab system**: 페이지를 탭으로 열어 navbar에 표시. 닫기 가능.

### Global UI Patterns
- **Component library**: Ant Design (buttons, inputs, selects, tables, switches) + styled-components
- **Theme**: Light mode (body class="light"), dark mode 지원
- **Layout**: 탭 navbar (44px height) + 콘텐츠 영역 (sidebar + main 패턴)
- **Color scheme (light)**: 흰 배경, green primary (#00b96b), gray borders
- **Font**: system font stack (Ubuntu, -apple-system, BlinkMacSystemFont, Segoe UI, ...)
- **Empty states**: 폴더 아이콘 + "No knowledge base found", "No results found" 등
- **DnD**: react-beautiful-dnd (탭 정렬, assistant 정렬)

---

## Screen: #/ (root) — Home / Chat

**Layout**: Navbar(top) + Sidebar(left: Assistants/Topics 탭 전환) + Chat Main

### UI Elements
- **Sidebar 좌측**: "Assistants" / "Topics" 탭 전환
  - Assistants 탭: "+ Add Assistant" 버튼, assistant 리스트 (emoji + 이름 + 컨텍스트 메뉴)
  - Topics 탭: Search input, topic 리스트 (드래그 가능)
- **Navbar 중앙**: 현재 assistant 이름 + model 이름 (클릭 가능)
- **Chat area**: 메시지 리스트 + InputBar (하단)
- **InputBar**: textarea ("Type your message here, press Enter to send") + 툴바 버튼들 (파일첨부, 웹검색, 글로벌, 코드, @mention, 이미지, 카메라 등)

### User Flows from This Screen
| Flow | Steps | Observations |
|------|-------|--------------|
| 새 대화 시작 | InputBar에 메시지 입력 → Enter | 스트리밍 응답 표시 |
| Assistant 전환 | Sidebar에서 다른 assistant 클릭 | 새 topic 생성 또는 기존 topic 로드 |
| Model 전환 | Navbar 중앙 model 이름 클릭 | Dropdown으로 model 선택 |
| Topic 관리 | Topics 탭 → topic 우클릭 | 컨텍스트 메뉴 (rename, delete, etc.) |

### Notes
- 기본 assistant: "Default Assistant" (emoji: 😀)
- Model 표시: "Qwen3-Next-80B | CherryIN"
- Welcome 메시지: "Hello, I'm Default Assistant. You can start chatting with me right away"

---

## Screen: #/settings/provider — Settings > Model Provider

**Layout**: Navbar(top) + Settings sidebar(left) + Provider list(center) + Provider detail(right)

### UI Elements
- **Settings sidebar**: Model Provider, Default Model, General Settings, Display Settings, Data Settings, MCP Servers, Web Search, Memories, API Server, Document Processing, Quick Phrases, Keyboard Shortcuts, Quick Assistant
- **Provider list** (중앙): 검색바 + 프로바이더 리스트 (CherryIN, SiliconFlow, AiHubMix, ocoolAI, BigModel, DeepSeek, Alaya NeW, DMXAPI, AiOnly, BurnCloud, TokenFlux + "Add" 버튼)
- **Provider detail** (우측): 이름, ON/OFF 토글, Login 버튼, API Key (password input + "Get API Key" link + "Check" 버튼), API Host (text input + preview URL), Models (Search + "Manage" + "Add" 버튼)

### User Flows from This Screen
| Flow | Steps | Observations |
|------|-------|--------------|
| 프로바이더 활성화 | 리스트에서 선택 → 토글 ON | 녹색 토글 표시 |
| API key 설정 | API Key 필드에 입력 → Check | 유효성 검증 |
| 모델 관리 | Models 섹션 → Manage/Add | 모델 추가/삭제 |
| 프로바이더 추가 | "+ Add" 버튼 | 새 프로바이더 생성 |

---

## Screen: #/settings/model — Settings > Default Model

**Layout**: Navbar(top) + Settings sidebar(left) + Model 설정 폼(center)

### UI Elements
- 3개의 Select/Dropdown (ant-select): model 관련 기본값 설정
- 검색 가능한 select 컴포넌트

---

## Screen: #/knowledge — Knowledge Base

**Layout**: Navbar(top) + Sidebar(left: KB 리스트) + Main(center: KB 내용)

### UI Elements
- **Sidebar**: "+ Add" 버튼 + KB 리스트
- **Main**: empty state → "No knowledge base found" (폴더 아이콘)

### Notes
- KB 생성 시 이름, 임베딩 모델, 차원 설정 필요 (코드 분석 기반)

---

## Screen: #/files — Files

**Layout**: Navbar(top) + Sidebar(left: 파일 타입 필터) + Main(center: 파일 리스트)

### UI Elements
- **Sidebar**: Document, Image, Text, All Files (카테고리 필터)
- **Main header**: Created At (정렬), Size, Name + "Select All" + 더보기(...)
- **Main content**: empty state → "暂无数据" (no data)

---

## Screen: #/translate — Translate

**Layout**: Navbar(top) + Split pane (좌: 입력, 우: 출력)

### UI Elements
- **Header bar**: OCR 카메라 아이콘, Source language dropdown ("Auto Detect"), 전환 화살표, Target language dropdown ("English"), "Translate" 버튼, model 선택 아이콘, 설정 아이콘
- **Left pane**: textarea ("Text, text files, or images (with OCR support) can be pasted or dragged in") + 글자 수 표시 (192)
- **Right pane**: "Translation" 레이블 + 결과 영역

---

## Screen: #/paintings — Paintings (Image Generation)

**Layout**: Navbar(top) + Sidebar(left: provider+model 선택) + Main(center: 이미지 캔버스 + 입력)

### UI Elements
- **Sidebar**: Provider dropdown (CherryIN 선택), "Learn More" 링크, 모델 선택 안내 ("No available image generation model, please add a model...")
- **Main header**: "Draw" / "Edit" 세그먼트 탭
- **Canvas area**: "No image available" (프린터 아이콘)
- **Bottom input**: textarea ("Enter your image description, text drawing uses 'double quotes' to wrap") + AI 아이콘 + 전송 버튼
- **우측**: 이미지 히스토리 (썸네일 그리드)

---

## Screen: #/code — Code Tools

**Layout**: Navbar(top) + Form (center)

### UI Elements
- **Heading**: "Code Tools" + "Quickly launch multiple code CLI tools to improve development efficiency"
- **Warning banner**: "Bun environment is required to run CLI tools" + "Install Bun" 버튼
- **Form fields**:
  - CLI Tool: Dropdown (기본값: "Qwen Code")
  - Model: Dropdown ("Select the model to use")
  - Working Directory: Dropdown + "Select Folder" 버튼
  - Environment Variables: textarea ("KEY1=value1\nKEY2=value2")
  - "Launch" 버튼

---

## Screen: #/store — Assistant Store

**Layout**: Navbar(top) + Sidebar(left: "我的"(My) / "精选"(Featured) 탭) + Main(center)

### UI Elements
- **Sub-tabs**: 我的(My), 精选(Featured)
- **Action buttons**: Search, Import from External, Manage Assistants, + Create Assistant
- **Main content**: empty state → "No results found" (프린터 아이콘)

### Notes
- 중국어와 영어 혼용 ("我的" = My, "精选" = Featured)

---

## Screen: #/notes — Notes

**Layout**: Navbar(top) + 빈 콘텐츠 영역

### Notes
- 스크린샷에서는 빈 화면으로 표시 (route가 별도 콘텐츠 없이 로드)

---

## Screen: #/history — History

**Layout**: Navbar(top) + 빈 콘텐츠 영역

### Notes
- 대화 히스토리 뷰 (빈 상태에서는 콘텐츠 없음)

---

## Runtime Default Verification

| Setting | Code Analysis | Runtime | Status |
|---------|--------------|---------|--------|
| Theme | light | light (body.class="light") | ✅ Confirmed |
| Navigation mode | tab (top navbar) | tab (top navbar) | ✅ Confirmed |
| Navbar height | 44px | 44px (--navbar-height) | ✅ Confirmed |
| Sidebar width | 50px | 50px (--sidebar-width) | ✅ Confirmed |
| Assistants panel width | 275px | 275px (--assistants-width) | ✅ Confirmed |
| Topic list width | 275px | 275px (--topic-list-width) | ✅ Confirmed |
| Settings panel width | 250px | 250px (--settings-width) | ✅ Confirmed |
| Primary color | #00b96b | #00b96b (--color-primary) | ✅ Confirmed |
| Font family | system stack | system stack (--font-family) | ✅ Confirmed |
| Border radius | .625rem | .625rem (--radius) | ✅ Confirmed |
